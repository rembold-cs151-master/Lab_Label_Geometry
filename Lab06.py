
from pgl import GWindow, GLine, GLabel

def visualize_label(text):
    """ 
    Function to draw a label centered to the screen with the
    baseline, ascent, and descent visualized by lines.

    Inputs:
        text (str): The string to be displayed as a label

    Outputs:
        None
    """
    # Add your code below!
    # Make sure it is indented to be part of the function!















if __name__ == '__main__':
    visualize_label("Everything is fine.")

